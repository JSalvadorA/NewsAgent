# requirements.txt

# PDF Processing
PyMuPDF>=1.19.0 # O la versión más reciente compatible
PyPDF2>=3.0.0  # Para extracción de texto de PDFs
pdf2image>=1.16.0  # Para conversión de PDF a imágenes

# OCR & Image Processing
pytesseract>=0.3.8  # OCR con Tesseract
opencv-python>=4.5.0  # Procesamiento de imágenes (cv2)
Pillow>=8.0.0  # Manipulación de imágenes
numpy>=1.19.0  # Necesario para operaciones con imágenes

# Audio Processing
whisper>=1.1.10  # Transcripción de audio local
SpeechRecognition>=3.8.0  # Reconocimiento de voz
vosk>=0.3.30  # Transcripción alternativa

# Data Handling
pandas>=1.3.0
PyYAML>=5.4.0

# Web Scraping & HTTP Requests
requests>=2.25.0
beautifulsoup4>=4.9.0
lxml>=4.6.0 # Parser alternativo para bs4, a menudo más rápido
selenium>=4.0.0
webdriver-manager>=3.5.0 # Para gestionar chromedriver automáticamente

# Otros (si se usan explícitamente, aunque algunos son dependencias)
urllib3>=1.26.0

# Para desarrollo y pruebas (opcional, pero recomendado)
# pytest>=6.2.0
